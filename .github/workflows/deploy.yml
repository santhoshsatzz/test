name: multi-branch

on:
  push:
    branches:
      - prod
      - dev
      - qa
      - test

jobs:
  prod-deploy:
  if: github.ref == 'refs/heads/prod'
  runs-on: ubuntu-latest
  steps:
    - name: checkout code
      uses: actions/checkout@v4
    - name: Prod message
      run: |
        echo "doing the cahnges in live prod"

  QA-deploy:
  if: github.ref == 'refs/heads/QA'
  runs-on: ubuntu-latest
  steps:
    - name: checkout code
      uses: actions/checkout@v4
    - name: QA message
      run: |
        echo "doing the changes in  QA"
